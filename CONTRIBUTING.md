# 贡献指南

感谢参与公共数据 Agent 平台的改进。

## 开发流程

1. 从 `main` 创建功能分支。
2. 保持改动聚焦，并为行为变化补充测试。
3. 后端通过 pytest 与 Ruff，前端通过 Vitest 与生产构建。
4. 数据库结构变化必须提供 Alembic migration。
5. Pull Request 说明变更内容、影响范围和验证方式。

提交标题建议使用 `feat:`、`fix:`、`test:`、`docs:`、`refactor:` 或 `chore:` 前缀。

## 数据与安全

- 不要提交 `.env`、API Key、密码、Token、连接串或个人路径。
- 不要提交来源许可不明确的政策文件、网页快照、统计资料或数据集。
- 测试优先使用代码内构造的最小合成数据。
- 安全问题请通过仓库的私密安全报告渠道提交，不要公开利用细节。
